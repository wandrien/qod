// TEST: compilation_should_fail 'static_assert: условие не выполнено'

include "../include/test_env.qdi";

int a, b;

static_assert(sizeof(a) != sizeof(a));

word main() of
	puts("OK1 ");
	return 0;
end
