// TEST: compilation_should_fail 'static_assert: выражение невычислимо во время компиляции'

include "../include/test_env.qdi";

int a, b;

static_assert(sizeof(a) == b);

word main() of
	puts("OK1 ");
	return 0;
end
